/**
 * Paquete de interfaces del dominio CRISS.
 *
 * <p>Contiene {@link interfaces.InterpolaridadDeDatos}, el contrato que deben
 * implementar todos los modelos que soporten persistencia en ficheros. Esta interfaz
 * define los métodos de serialización (loadTo*) y deserialización (objFrom*) para
 * los formatos CSV, JSON, TXT y binario serializado.</p>
 *
 * @author isard
 * @version 1.0
 */
package interfaces;
